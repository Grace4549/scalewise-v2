Fix the expert-to-client reschedule request flow end to end:
1. Fix the email reschedule link:
When an expert requests to reschedule and the client receives a notification email containing a "Pick a New Time" button, that button must direct the client to the expert's availability calendar on ScaleWise where they can select a new time slot for that specific booking. Currently the link is directing to a page that requires login and shows "page not found." Fix this by directing the link to a dedicated reschedule page at a URL such as /reschedule/[booking-id] that works as follows: if the client is already logged in, it shows the expert's calendar immediately. If the client is not logged in, it shows a simple login prompt first, then redirects to the expert's calendar after login, not to a generic page not found error.
2. Add three clear action options for the client:
In both the reschedule notification email and the in-app notification, give the client exactly three options presented as clearly labeled buttons:

"Pick a New Time" — directs the client to the expert's live availability calendar to select a new slot. Once a new time is selected and confirmed, the booking is updated to the new time, both parties are notified, and the original time slot is released back to the expert's availability.
"Keep Original Time" — client declines the reschedule request and wants to proceed with the originally booked session. Notifies the expert via email and in-app notification that the client has declined the reschedule and expects the session to proceed at the original time. The expert must then either show up at the original time or formally cancel the booking, which would trigger a full refund to the client per our cancellation policy.
"Cancel This Booking" — client decides they no longer want the session at all. Triggers the standard cancellation flow with the appropriate refund calculation based on how much notice is being given relative to the original session time, using our refund anchor rule (calculated from whichever session time came first, original or rescheduled).

3. Fix in-app reschedule notification:
The in-app notification on the client side when an expert requests a reschedule must include the same three action buttons directly within the notification card, not just a text description. "Pick a New Time" should navigate to the expert's calendar for that specific booking. "Keep Original Time" and "Cancel This Booking" should trigger their respective flows immediately. The notification should clearly state which expert sent the request and what the original booking date and time was