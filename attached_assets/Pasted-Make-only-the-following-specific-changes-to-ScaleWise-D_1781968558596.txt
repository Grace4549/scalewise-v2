Make only the following specific changes to ScaleWise. Do not modify, restyle, or rearrange anything else on the site that is not explicitly mentioned below.
1. Reviews section — convert to a center mode testimonial carousel:

Replace the current static reviews grid with a center mode carousel. The review card in the center should appear larger and more prominent (slightly scaled up, full opacity), while review cards on either side appear smaller and slightly faded or dimmed, similar to a coverflow style effect. The carousel should auto rotate continuously, sliding left at a slow, comfortable pace, and also allow users to manually click left and right arrows or swipe to move through reviews themselves. Pause the auto rotation briefly when a user manually interacts with it, then resume after a few seconds. Keep the existing "Leave a Review" button and its form with the same relevant questions it currently asks. If there are zero reviews yet, keep the existing empty state message and "Leave a Review" button instead of showing an empty or broken carousel.
2. Existing reviews — remove company names. For any reviews already populating the site, remove the business or company name field from display, keeping only the reviewer's name and their review text/rating.
3. Industries section — keep cards fixed in place, do not animate or rotate them. The industries grid should remain static, not moving or cycling. Differentiate each industry card using a distinct color and icon per industry, drawing from our existing brand palette. Add a search bar within the Industries section that lets users search by industry name, with autocomplete suggestions appearing as they type, filtering the visible industries in real time.
4. Add relevant stock photography to the homepage in appropriate sections (such as the hero section and any sections currently missing imagery), following the same guidance as before: professional, relevant images representing business owners or coaching/consultation moments, no real named individuals, and nothing implying we currently have real onboarded experts.
Apply only these four changes. Leave all other existing layouts, colors, copy, animations, and functionality exactly as they currently are.